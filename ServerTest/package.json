{
  "name": "viewhub-server-tests",
  "version": "1.0.0",
  "description": "Test suite for ViewHub GraphQL API server",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:schema": "jest --testPathPattern=schemaValidation",
    "test:external": "jest --testPathPattern=externalApiSchema"
  },
  "dependencies": {
    "@apollo/server": "^5.0.0",
    "axios": "^1.12.2",
    "fs-extra": "^11.3.2",
    "graphql": "^16.8.1",
    "graphql-tag": "^2.12.6",
    "jest": "^29.7.0",
    "jest-environment-node": "^29.7.0",
    "node-fetch": "^2.7.0",
    "supertest": "^7.1.3"
  },
  "devDependencies": {
    "@types/jest": "^29.5.8",
    "@types/supertest": "^2.0.16"
  }
}
